package biglib

//with GetRunningSolutionProcessor(s *Solution) *ChannelController
// ensure return is complete
// acceptance tests against edge case Problems
//with GetIdleSolutionProcessor(poolid string, s *Solution, stateChannel *chan ProcessControlOperation, resultChannel *chan SimulationState)
// ensure return is not started
// ensure return has hash with correct format
// ensure return has name with correct format
// ensure stateChannel is registered
// ensure resultChannel is registered
