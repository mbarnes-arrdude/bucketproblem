package biglib

//with (s *Solution) generateSimulation(controller *ChannelController)
// ensure with assurance test the solutions bucketcache across a bracket of each problem value 0x0 to 0xff
// ensure with assurance test the solutions bucketcache across several very large values (with short simulations)
